import React from "react";
import { View, StyleSheet, Text, Button } from "react-native";

const EditLogScreen = () => {
  return <Text style={{ fontSize: 48 }}>EditLogScreen</Text>;
};

const styles = StyleSheet.create({});

export default EditLogScreen;
